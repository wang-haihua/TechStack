# 'wget' is used to download files from web server, like 'wget URL'
wget http://slynux.org

# you also can specify multiple URLs to download different files, like 'wget URL1 URL2 URL3...'

# if the website need HTTP/FTP certification, you can use the command like "wget --user username --password pass URL"
